import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
export interface MCPConnection {
    client: Client;
    transport: StdioClientTransport;
}
/**
 * Connect to an external MCP server via stdio
 */
export declare function connectToMCPServer(command: string, args?: string[], options?: {
    cwd?: string;
    env?: Record<string, string>;
}): Promise<MCPConnection>;
/**
 * Check if a tool is available on the connected MCP server
 */
export declare function listServerTools(connection: MCPConnection): Promise<string[]>;
/**
 * Call a tool on the connected MCP server
 */
export declare function callServerTool(connection: MCPConnection, toolName: string, args: Record<string, unknown>): Promise<unknown>;
/**
 * Disconnect from MCP server and cleanup
 */
export declare function disconnectFromMCPServer(connection: MCPConnection): Promise<void>;
