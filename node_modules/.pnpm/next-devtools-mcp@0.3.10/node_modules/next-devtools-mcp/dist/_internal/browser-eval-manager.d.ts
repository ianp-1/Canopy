import { MCPConnection } from "./mcp-client.js";
/**
 * Ensure playwright-mcp is installed and install if needed
 */
export declare function ensureBrowserEvalMCP(): Promise<void>;
/**
 * Start playwright-mcp server and connect to it
 */
export declare function startBrowserEvalMCP(options?: {
    browser?: "chrome" | "firefox" | "webkit" | "msedge";
    headless?: boolean;
}): Promise<MCPConnection>;
/**
 * Get the current browser eval connection
 */
export declare function getBrowserEvalConnection(): MCPConnection | null;
/**
 * Stop playwright-mcp server and cleanup
 */
export declare function stopBrowserEvalMCP(): Promise<void>;
